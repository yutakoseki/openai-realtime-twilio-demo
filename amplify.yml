version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd webapp
        - npm ci
    build:
      commands:
        - cd webapp
        - npm run build
  artifacts:
    baseDirectory: webapp/out
    files:
      - '**/*'
  cache:
    paths:
      - webapp/node_modules/**/* 